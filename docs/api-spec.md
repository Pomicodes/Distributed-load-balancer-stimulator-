# HTTP API overview

The FastAPI app exposes health checks, WebSocket metrics, simulation controls, and configuration endpoints. Inspect `backend/app/main.py` and the `backend/tests/test_api.py` suite for the live contract.
