# Load balancing algorithms

This simulator supports pluggable strategies (for example round-robin and least-connections). See `backend/app/algorithms/` for implementations and tests under `backend/tests/`.
